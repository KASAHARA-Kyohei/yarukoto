# yarukoto 手動確認チェックリスト

## 起動前

- Node.js の警告が出る場合があります。現在の環境が `22.11.0` の場合、Vite は `22.12+` を推奨します。
- 警告が出ても `npm run build` は通っています。

## 起動

```bash
npm run tauri dev
```

## 基本確認

- アプリが起動する。
- 左ペインにプロジェクト一覧が表示される。
- 中央ペインにツリーが表示される。
- 右側の常時Detailペインがなく、中央Workspaceが広く表示される。
- ノードをクリックしても編集ウィンドウは開かず、選択だけされる。
- 初回起動時、サンプルデータが表示される。

## SQLite永続化

1. 任意のノードを選択する。
2. `Enter` / `i` / `編集` ボタンで編集ウィンドウを開く。
3. 編集ウィンドウで `title`, `status`, `memo`, `開始日`, `終了日` を変更する。
4. 編集ウィンドウ下部の `save: saved` を確認する。
5. アプリを終了する。
6. 再度 `npm run tauri dev` で起動する。
7. 変更内容が残っていることを確認する。

## 通常操作

- 左ペインの `ルート追加` で新しいプロジェクトが作成される。
- 中央ペインの `子追加` で選択ノードの子が作成される。
- `下追加` で同階層の下にノードが作成される。
- `編集` で選択ノードの編集ウィンドウが開く。
- `上へ` / `下へ` で同階層内の順序が変わる。
- `子階層へ` で直前の同階層ノードの子になる。
- `親階層へ` で親の次の兄弟になる。
- `削除` で選択ノードと子孫が削除される。

## Vimライク操作

入力欄にフォーカスしていない状態で確認します。

- `j` / `k`: 選択ノードが上下に移動する。
- `↑` / `↓`: 選択ノードが上下に移動する。
- `Ctrl+h` / `Ctrl+l`: `Projects` / `Center` が切り替わる。
- `h`: 展開中なら折りたたむ。折りたたみ済みなら親へ移動する。
- `l`: 子があれば展開する。展開済みなら最初の子へ移動する。
- `←`: 展開中なら折りたたむ。折りたたみ済みなら親へ移動する。
- `→`: 子があれば展開する。展開済みなら最初の子へ移動する。
- `i` / `Enter`: 編集ウィンドウを開き、タイトル入力にフォーカスする。
- `a`: 子ノードを追加する。
- `o`: 下に同階層ノードを追加する。
- `dd`: 選択ノードと子孫を削除する。
- `J` / `K`: ノードを下/上に移動する。
- `>` / `<`: 子階層/親階層へ移動する。
- 編集ウィンドウ内の `j` / `k`: 編集項目を移動する。
- 編集ウィンドウ内の `i` / `Enter`: 選択中の編集項目へフォーカスする。
- 編集ウィンドウ内の `Esc`: 入力欄フォーカス中はフォーカス解除、フォーム外ではウィンドウを閉じる。

## ビュー切替

中央ペイン上部のタブを確認します。

- `Tree`: ツリー操作ができる。
- `Kanban`: 全Taskが `Inbox / Next / Doing / Done` の4列に表示される。
- `Calendar`: `終了日` があるノードが月表示される。
- `Report`: 総数、未完了、期限切れ、7日以内期限、種別/ステータス別集計が表示される。

## Kanban

- 折りたたまれた階層を含む全Taskが表示され、Task以外は表示されない。
- カードにタイトル、親パス、設定済みの場合は期限が表示される。
- `j` / `k` で同じ列、`h` / `l` で前後の空でない列へ選択が移動する。
- `↑` / `↓` で同じ列、`←` / `→` で前後の空でない列へ選択が移動する。
- `H` / `L` で選択カードだけが前後のstatusへ移動する。
- カードを別列へドラッグするとstatusが変更され、同じ列へのドロップでは変更されない。
- 親Taskを移動しても子Taskのstatusは変更されない。
- `f` のヒント入力で対象カードが選択され、画面外なら表示範囲へスクロールする。
- アプリ再起動後も変更したstatusが保持される。
- 横幅が狭い場合はボードを横スクロールできる。
- Light / Tokyo Night / Soft Light の各テーマで選択・列・カードが判別できる。

## 期間表示

- 編集ウィンドウで `開始日` と `終了日` を個別に設定/クリアできる。
- Tree 行の右側に `開始日 -> 終了日` と期間バーが表示される。
- `終了日` だけのノードは従来通り期限バッジとして表示される。
- `開始日 > 終了日` にしても保存され、編集ウィンドウと Tree 行に警告が表示される。
- アプリ再起動後も `開始日` と `終了日` が残っている。

## 重点確認シナリオ

1. ルートを追加する。
2. 子ノードを3つ追加する。
3. 2つ目の子ノードに `開始日` と `終了日` を設定する。
4. `Calendar` に終了日ベースで表示されることを確認する。
5. Tree の期間列に日付範囲と期間バーが出ることを確認する。
6. `Report` の件数が変わることを確認する。
7. ツリーに戻り、`J/K` と `>/<` で順序と階層を変更する。
8. 親ノードを削除し、子孫も消えることを確認する。
9. アプリを再起動し、残った変更が保存されていることを確認する。

## ペイン切替

- `Ctrl+h` で `Projects` へ移動し、`Ctrl+l` で `Center` へ戻る。
- `Tab` / `Shift+Tab` の既存ペイン移動も引き続き動作する。
- `Calendar` ビューでは `H / L` で前月 / 翌月、`Ctrl+h / Ctrl+l` はペイン切替になる。
- `Projects` では `↑` / `↓` でプロジェクト選択、`→` で `Center` へ移動できる。

## 既知の注意点

- 通知、クラウド同期、認証、複数ユーザーは未実装です。
- 保存失敗時は編集ウィンドウ下部に `save: error` とエラー内容が表示されます。
## LLMレビュー連携

- [ ] Treeビューでプロジェクトを選択し、`LLM用コピー` を押すと成功メッセージが表示される
- [ ] `y` でも同じ内容をコピーできる
- [ ] コピー内容に指示文と `yarukoto-tree` バージョン1のJSONコードブロックが含まれる
- [ ] 折りたたまれた子孫を含め、全ノードと全項目がJSONに含まれる
- [ ] JSONに `id`、`parentId`、`sortOrder`、作成・更新日時が含まれない
- [ ] LLMのJSONコードブロックをクリップボードへ入れ、`p` で取込ダイアログが開く
- [ ] 生JSONを直接貼り付けてもプレビューできる
- [ ] プレビューにルート名と総ノード数が表示される
- [ ] 項目不足、不正なtype/status、実在しない日付、異なるversionでは取込ボタンが無効になる
- [ ] 正常なJSONを取り込むと、元のプロジェクトを残して新しいプロジェクトが作成される
- [ ] 取り込んだプロジェクトの階層、並び順、各項目がJSONと一致する
- [ ] 取込処理が失敗した場合、途中までのノードが作成されない
